> [!NOTE] Definition
> In TCP, a term used to describe a TCP header and its encapsulated data (also called an L4PDU).
>  Also in TCP, the process of accepting a large chunk of data from the application layer and breaking it into smaller pieces that fit into TCP segments. 
>  In Ethernet, a segment is either a single Ethernet cable or a single collision domain (no matter how many cables are used).
