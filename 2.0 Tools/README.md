# 🔧 Tools

Reference cards per tool. Use `_templates/Template — Tool Reference`.

**Naming:** Use the tool name exactly.
Example: `Splunk.md` · `Wireshark.md` · `Sysmon.md`

Store commands you look up repeatedly.
This replaces googling the same thing twice.
